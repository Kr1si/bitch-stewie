# Markdown vault

Drop markdown/text files here. `make watch` auto-ingests anything you add or
edit into the Qdrant knowledge base (global collection, or set
`--project` via the CLI). `make ingest PATH=./vault` ingests the whole tree
on demand.