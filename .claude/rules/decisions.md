- **Deep Research: dedicated FE button -> CC /deep-research one-shot**: Add a direct deep-research path driven by an explicit FE button, separate from the chat orchestrator. A new SSE endpoint POST /api/research/deep/stream accepts {goal, project} and runs the CC worker's /deep-research one-shot (run_prompt) via asyncio.to_thread so the minutes-long blocking call doesn't stall the event loop. It emits start/done/error SSE events; the report is ingested into the Qdrant knowledge base (kind="research"). The existing deep_research @tool was refactored to delegate to a reusable run_deep_research(question, project) helper shared by the tool and the API, avoiding duplication. The FE gets a new "Deep Research" nav page (TravelExplore icon) at /research with a goal textarea, optional project selector, Run/Abort buttons, and a pre-wrapped Markdown report view. _(context: bitch-stewie is an orchestration layer over Claude Code. deep_research already existed as an orchestrator @tool (research_tools.py) invoked indirectly through chat. The user asked for an explicit FE button that takes a goal and outputs research through Claude Code, so a dedicated endpoint + page is cleaner than forcing it through the chat flow. The CC worker (cc_bridge/worker.py) blocks the calling thread (Proactor loop in its own thread), so the API handler must dispatch to a threadpool.)_
