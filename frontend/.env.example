# Copy to frontend/.env (gitignored). Base URL of the FastAPI backend.
# Default in code is http://localhost:8000 — override to the port you run
# `make backend` on (8000 is taken locally, so 8011 here).
VITE_API_BASE=http://localhost:8011